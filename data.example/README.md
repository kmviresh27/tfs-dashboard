# data/ folder structure

This folder is excluded from git (see .gitignore). Copy this folder to `data/` and edit as needed.

- `departments.json` — list of registered departments/tenants
- `departments/` — per-department config, snapshots, users
- `users.json` — global user list
- `user-pats.json` — per-user PAT tokens (sensitive!)
